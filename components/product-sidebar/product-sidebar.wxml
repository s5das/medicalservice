<view class="sidebar" bindtap="handleSwitch">
  <view wx:for="{{ items }}" wx:key="model_id" data-id="{{ index }}" class="sidebar-item {{ active === index ? 'active' :  ''}}">{{ item.type_name }}</view>
</view>
